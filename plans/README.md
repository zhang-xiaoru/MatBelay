Approved computation plans, one directory per exploration:
`plans/<exploration>/<YYYY-MM-DD>_<short-name>.md`.

Written by the `plan-computation` skill after the user approves a plan. Each
planning session gets its own dated file rather than editing an earlier one.
