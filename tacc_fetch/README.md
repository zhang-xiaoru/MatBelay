Results fetched back from the cluster — an exact mirror of `implementation/`:
`tacc_fetch/<exploration>/<level>_<id>_<node>/`.

Fetch **named files only**, never a whole directory. See
`.claude/rules/tacc-fetch-rule.md`. Contents are gitignored: these are outputs.
